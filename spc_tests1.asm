; Auto-generated by make_tests.py

include "spc_common.inc"
start_tests:

test01f4:
        mov a, #$f4
        mov y, #$01
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.1
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$fd
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01f5:
        mov a, #$f5
        mov y, #$01
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.1
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$fd
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01f6:
        mov a, #$f6
        mov y, #$01
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.1
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01f7:
        mov a, #$f7
        mov y, #$01
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.1
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01f8:
        mov a, #$f8
        mov y, #$01
        call init_test
        mov $01, #$ff
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.1
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$fd
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01f9:
        mov a, #$f9
        mov y, #$01
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.1
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$fd
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01fa:
        mov a, #$fa
        mov y, #$01
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.1
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01fb:
        mov a, #$fb
        mov y, #$01
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.1
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01fc:
        mov a, #$fc
        mov y, #$01
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.2
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$fb
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01fd:
        mov a, #$fd
        mov y, #$01
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.2
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$fb
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01fe:
        mov a, #$fe
        mov y, #$01
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.2
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test01ff:
        mov a, #$ff
        mov y, #$01
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.2
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0200:
        mov a, #$00
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.2
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$fb
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0201:
        mov a, #$01
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.2
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$fb
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0202:
        mov a, #$02
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.2
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0203:
        mov a, #$03
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.2
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0204:
        mov a, #$04
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.3
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$f7
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0205:
        mov a, #$05
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.3
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$f7
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0206:
        mov a, #$06
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.3
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0207:
        mov a, #$07
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.3
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0208:
        mov a, #$08
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.3
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$f7
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0209:
        mov a, #$09
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.3
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$f7
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test020a:
        mov a, #$0a
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.3
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test020b:
        mov a, #$0b
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.3
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test020c:
        mov a, #$0c
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.4
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ef
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test020d:
        mov a, #$0d
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.4
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ef
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test020e:
        mov a, #$0e
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.4
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test020f:
        mov a, #$0f
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.4
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0210:
        mov a, #$10
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.4
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ef
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0211:
        mov a, #$11
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.4
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ef
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0212:
        mov a, #$12
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.4
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0213:
        mov a, #$13
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.4
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0214:
        mov a, #$14
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.5
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$df
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0215:
        mov a, #$15
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.5
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$df
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0216:
        mov a, #$16
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.5
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0217:
        mov a, #$17
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.5
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0218:
        mov a, #$18
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.5
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$df
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0219:
        mov a, #$19
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.5
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$df
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test021a:
        mov a, #$1a
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.5
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test021b:
        mov a, #$1b
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.5
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test021c:
        mov a, #$1c
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.6
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$bf
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test021d:
        mov a, #$1d
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.6
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$bf
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test021e:
        mov a, #$1e
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.6
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test021f:
        mov a, #$1f
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.6
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0220:
        mov a, #$20
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.6
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$bf
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0221:
        mov a, #$21
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.6
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$bf
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0222:
        mov a, #$22
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.6
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0223:
        mov a, #$23
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.6
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0224:
        mov a, #$24
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.7
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0225:
        mov a, #$25
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.7
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0226:
        mov a, #$26
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.7
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0227:
        mov a, #$27
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.7
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0228:
        mov a, #$28
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.7
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0229:
        mov a, #$29
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.7
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test022a:
        mov a, #$2a
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $01.7
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test022b:
        mov a, #$2b
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clr1 $02.7
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test022c:
        mov a, #$2c
        mov y, #$02
        call init_test
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clrc
        call save_results ; now a = result psw
        cmp a, #$fe
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test022d:
        mov a, #$2d
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clrc
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test022e:
        mov a, #$2e
        mov y, #$02
        call init_test
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clrp
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test022f:
        mov a, #$2f
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clrp
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0230:
        mov a, #$30
        mov y, #$02
        call init_test
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clrv
        call save_results ; now a = result psw
        cmp a, #$b7
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0231:
        mov a, #$31
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        clrv
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0232:
        mov a, #$32
        mov y, #$02
        call init_test
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        setc
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0233:
        mov a, #$33
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        setc
        call save_results ; now a = result psw
        cmp a, #$01
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0234:
        mov a, #$34
        mov y, #$02
        call init_test
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        setp
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0235:
        mov a, #$35
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        setp
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0236:
        mov a, #$36
        mov y, #$02
        call init_test
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        di
        call save_results ; now a = result psw
        cmp a, #$fb
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0237:
        mov a, #$37
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        di
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0238:
        mov a, #$38
        mov y, #$02
        call init_test
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        ei
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0239:
        mov a, #$39
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        ei
        call save_results ; now a = result psw
        cmp a, #$04
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test023a:
        mov a, #$3a
        mov y, #$02
        call init_test
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        notc
        call save_results ; now a = result psw
        cmp a, #$fe
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test023b:
        mov a, #$3b
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        notc
        call save_results ; now a = result psw
        cmp a, #$01
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test023c:
        mov a, #$3c
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, #$12
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test023d:
        mov a, #$3d
        mov y, #$02
        call init_test
        mov $01, #$12
        mov $02, #$12
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$02
        pop psw
        cmp (x), (y)
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        cmp $02, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test023e:
        mov a, #$3e
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$56
        pop psw
        cmp a, (x)
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test023f:
        mov a, #$3f
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$12
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$05
        pop psw
        cmp a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$12
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0240:
        mov a, #$40
        mov y, #$02
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$12
        mov $0102, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        cmp a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0241:
        mov a, #$41
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $01
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0242:
        mov a, #$42
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $02
        call save_results ; now a = result psw
        cmp a, #$23
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0243:
        mov a, #$43
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        cmp a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0244:
        mov a, #$44
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        cmp a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$23
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0245:
        mov a, #$45
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $103
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0246:
        mov a, #$46
        mov y, #$02
        call init_test
        mov $04, #$12
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$05
        mov y, #$56
        pop psw
        cmp a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0247:
        mov a, #$47
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0104, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$01
        pop psw
        cmp a, $103+y
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0248:
        mov a, #$48
        mov y, #$02
        call init_test
        mov $01, #$12
        mov $02, #$12
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $01, $02
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        cmp $02, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0249:
        mov a, #$49
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$12
        mov $0103, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $02, $03
        call save_results ; now a = result psw
        cmp a, #$23
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test024a:
        mov a, #$4a
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $01, #$12
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test024b:
        mov a, #$4b
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $02, #$12
        call save_results ; now a = result psw
        cmp a, #$23
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test024c:
        mov a, #$4c
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        cmp x, #$12
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test024d:
        mov a, #$4d
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        cmp x, $01
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test024e:
        mov a, #$4e
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        cmp x, $02
        call save_results ; now a = result psw
        cmp a, #$23
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test024f:
        mov a, #$4f
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        cmp x, $103
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0250:
        mov a, #$50
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        cmp y, #$12
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0251:
        mov a, #$51
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        cmp y, $01
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0252:
        mov a, #$52
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        cmp y, $02
        call save_results ; now a = result psw
        cmp a, #$23
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0253:
        mov a, #$53
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        cmp y, $103
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0254:
        mov a, #$54
        mov y, #$02
        call init_test
        mov a, #$df
        push a
        mov a, #$13
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, #$12
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0255:
        mov a, #$55
        mov y, #$02
        call init_test
        mov $01, #$13
        mov $02, #$12
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$02
        pop psw
        cmp (x), (y)
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$13
        bne .to_fail
        cmp $02, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0256:
        mov a, #$56
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$13
        mov x, #$01
        mov y, #$56
        pop psw
        cmp a, (x)
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0257:
        mov a, #$57
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$12
        mov a, #$df
        push a
        mov a, #$13
        mov x, #$34
        mov y, #$05
        pop psw
        cmp a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$12
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0258:
        mov a, #$58
        mov y, #$02
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$12
        mov $0102, a
        mov a, #$df
        push a
        mov a, #$13
        mov x, #$02
        mov y, #$56
        pop psw
        cmp a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0259:
        mov a, #$59
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$13
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $01
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test025a:
        mov a, #$5a
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$13
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $02
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test025b:
        mov a, #$5b
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$13
        mov x, #$02
        mov y, #$56
        pop psw
        cmp a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test025c:
        mov a, #$5c
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$13
        mov x, #$03
        mov y, #$56
        pop psw
        cmp a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test025d:
        mov a, #$5d
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$13
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $103
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test025e:
        mov a, #$5e
        mov y, #$02
        call init_test
        mov $04, #$12
        mov a, #$df
        push a
        mov a, #$13
        mov x, #$05
        mov y, #$56
        pop psw
        cmp a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test025f:
        mov a, #$5f
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0104, a
        mov a, #$df
        push a
        mov a, #$13
        mov x, #$34
        mov y, #$01
        pop psw
        cmp a, $103+y
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$13
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0260:
        mov a, #$60
        mov y, #$02
        call init_test
        mov $01, #$13
        mov $02, #$12
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $01, $02
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$13
        bne .to_fail
        cmp $02, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0261:
        mov a, #$61
        mov y, #$02
        call init_test
        mov a, #$13
        mov $0102, a
        mov a, #$12
        mov $0103, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $02, $03
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$13
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0262:
        mov a, #$62
        mov y, #$02
        call init_test
        mov $01, #$13
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $01, #$12
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0263:
        mov a, #$63
        mov y, #$02
        call init_test
        mov a, #$13
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $02, #$12
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0264:
        mov a, #$64
        mov y, #$02
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$13
        mov y, #$56
        pop psw
        cmp x, #$12
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$13
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0265:
        mov a, #$65
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$13
        mov y, #$56
        pop psw
        cmp x, $01
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$13
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0266:
        mov a, #$66
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$13
        mov y, #$56
        pop psw
        cmp x, $02
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$13
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0267:
        mov a, #$67
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$13
        mov y, #$56
        pop psw
        cmp x, $103
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$13
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0268:
        mov a, #$68
        mov y, #$02
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$13
        pop psw
        cmp y, #$12
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0269:
        mov a, #$69
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$13
        pop psw
        cmp y, $01
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$13
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test026a:
        mov a, #$6a
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$13
        pop psw
        cmp y, $02
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$13
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test026b:
        mov a, #$6b
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$13
        pop psw
        cmp y, $103
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$13
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test026c:
        mov a, #$6c
        mov y, #$02
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, #$13
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test026d:
        mov a, #$6d
        mov y, #$02
        call init_test
        mov $01, #$12
        mov $02, #$13
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$02
        pop psw
        cmp (x), (y)
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        cmp $02, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test026e:
        mov a, #$6e
        mov y, #$02
        call init_test
        mov $01, #$13
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$56
        pop psw
        cmp a, (x)
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test026f:
        mov a, #$6f
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$13
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$05
        pop psw
        cmp a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$13
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0270:
        mov a, #$70
        mov y, #$02
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$13
        mov $0102, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        cmp a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$13
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0271:
        mov a, #$71
        mov y, #$02
        call init_test
        mov $01, #$13
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $01
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0272:
        mov a, #$72
        mov y, #$02
        call init_test
        mov a, #$13
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $02
        call save_results ; now a = result psw
        cmp a, #$fc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0273:
        mov a, #$73
        mov y, #$02
        call init_test
        mov $01, #$13
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        cmp a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0274:
        mov a, #$74
        mov y, #$02
        call init_test
        mov a, #$13
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        cmp a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$fc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0275:
        mov a, #$75
        mov y, #$02
        call init_test
        mov a, #$13
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $103
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0276:
        mov a, #$76
        mov y, #$02
        call init_test
        mov $04, #$13
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$05
        mov y, #$56
        pop psw
        cmp a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0277:
        mov a, #$77
        mov y, #$02
        call init_test
        mov a, #$13
        mov $0104, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$01
        pop psw
        cmp a, $103+y
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0278:
        mov a, #$78
        mov y, #$02
        call init_test
        mov $01, #$12
        mov $02, #$13
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $01, $02
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        cmp $02, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0279:
        mov a, #$79
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$13
        mov $0103, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $02, $03
        call save_results ; now a = result psw
        cmp a, #$fc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        mov a, $0103
        cmp a, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test027a:
        mov a, #$7a
        mov y, #$02
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $01, #$13
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test027b:
        mov a, #$7b
        mov y, #$02
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $02, #$13
        call save_results ; now a = result psw
        cmp a, #$fc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test027c:
        mov a, #$7c
        mov y, #$02
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        cmp x, #$13
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test027d:
        mov a, #$7d
        mov y, #$02
        call init_test
        mov $01, #$13
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        cmp x, $01
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test027e:
        mov a, #$7e
        mov y, #$02
        call init_test
        mov a, #$13
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        cmp x, $02
        call save_results ; now a = result psw
        cmp a, #$fc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test027f:
        mov a, #$7f
        mov y, #$02
        call init_test
        mov a, #$13
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        cmp x, $103
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0280:
        mov a, #$80
        mov y, #$02
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        cmp y, #$13
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0281:
        mov a, #$81
        mov y, #$02
        call init_test
        mov $01, #$13
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        cmp y, $01
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        cmp $01, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0282:
        mov a, #$82
        mov y, #$02
        call init_test
        mov a, #$13
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        cmp y, $02
        call save_results ; now a = result psw
        cmp a, #$fc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        mov a, $0102
        cmp a, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0283:
        mov a, #$83
        mov y, #$02
        call init_test
        mov a, #$13
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        cmp y, $103
        call save_results ; now a = result psw
        cmp a, #$dc
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        mov a, $0103
        cmp a, #$13
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0284:
        mov a, #$84
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, #$ff
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0285:
        mov a, #$85
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$02
        pop psw
        cmp (x), (y)
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0286:
        mov a, #$86
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$01
        mov y, #$56
        pop psw
        cmp a, (x)
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0287:
        mov a, #$87
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$ff
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$05
        pop psw
        cmp a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$ff
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0288:
        mov a, #$88
        mov y, #$02
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$ff
        mov $0102, a
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$02
        mov y, #$56
        pop psw
        cmp a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0289:
        mov a, #$89
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test028a:
        mov a, #$8a
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $02
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test028b:
        mov a, #$8b
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$02
        mov y, #$56
        pop psw
        cmp a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test028c:
        mov a, #$8c
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$fe
        mov x, #$03
        mov y, #$56
        pop psw
        cmp a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test028d:
        mov a, #$8d
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $103
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test028e:
        mov a, #$8e
        mov y, #$02
        call init_test
        mov $04, #$ff
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$05
        mov y, #$56
        pop psw
        cmp a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test028f:
        mov a, #$8f
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0104, a
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$01
        pop psw
        cmp a, $103+y
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0290:
        mov a, #$90
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $01, $02
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0291:
        mov a, #$91
        mov y, #$02
        call init_test
        mov a, #$fe
        mov $0102, a
        mov a, #$ff
        mov $0103, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $02, $03
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$fe
        bne .to_fail
        mov a, $0103
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0292:
        mov a, #$92
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $01, #$ff
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0293:
        mov a, #$93
        mov y, #$02
        call init_test
        mov a, #$fe
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $02, #$ff
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$fe
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0294:
        mov a, #$94
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$fe
        mov y, #$56
        pop psw
        cmp x, #$ff
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$fe
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0295:
        mov a, #$95
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$fe
        mov y, #$56
        pop psw
        cmp x, $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$fe
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0296:
        mov a, #$96
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$fe
        mov y, #$56
        pop psw
        cmp x, $02
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$fe
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0297:
        mov a, #$97
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$fe
        mov y, #$56
        pop psw
        cmp x, $103
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$fe
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0298:
        mov a, #$98
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$fe
        pop psw
        cmp y, #$ff
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$fe
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0299:
        mov a, #$99
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$fe
        pop psw
        cmp y, $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$fe
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test029a:
        mov a, #$9a
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$fe
        pop psw
        cmp y, $02
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$fe
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test029b:
        mov a, #$9b
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$fe
        pop psw
        cmp y, $103
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$fe
        bne .to_fail
        mov a, $0103
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test029c:
        mov a, #$9c
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, #$01
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test029d:
        mov a, #$9d
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$01
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$02
        pop psw
        cmp (x), (y)
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test029e:
        mov a, #$9e
        mov y, #$02
        call init_test
        mov $01, #$01
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$01
        mov y, #$56
        pop psw
        cmp a, (x)
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test029f:
        mov a, #$9f
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$01
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$05
        pop psw
        cmp a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$01
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02a0:
        mov a, #$a0
        mov y, #$02
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$01
        mov $0102, a
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$02
        mov y, #$56
        pop psw
        cmp a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$01
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02a1:
        mov a, #$a1
        mov y, #$02
        call init_test
        mov $01, #$01
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $01
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02a2:
        mov a, #$a2
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $02
        call save_results ; now a = result psw
        cmp a, #$a1
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02a3:
        mov a, #$a3
        mov y, #$02
        call init_test
        mov $01, #$01
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$02
        mov y, #$56
        pop psw
        cmp a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02a4:
        mov a, #$a4
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$fe
        mov x, #$03
        mov y, #$56
        pop psw
        cmp a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$a1
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02a5:
        mov a, #$a5
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$56
        pop psw
        cmp a, $103
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02a6:
        mov a, #$a6
        mov y, #$02
        call init_test
        mov $04, #$01
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$05
        mov y, #$56
        pop psw
        cmp a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02a7:
        mov a, #$a7
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0104, a
        mov a, #$00
        push a
        mov a, #$fe
        mov x, #$34
        mov y, #$01
        pop psw
        cmp a, $103+y
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$fe
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02a8:
        mov a, #$a8
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$01
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $01, $02
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02a9:
        mov a, #$a9
        mov y, #$02
        call init_test
        mov a, #$fe
        mov $0102, a
        mov a, #$01
        mov $0103, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $02, $03
        call save_results ; now a = result psw
        cmp a, #$a1
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$fe
        bne .to_fail
        mov a, $0103
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02aa:
        mov a, #$aa
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $01, #$01
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ab:
        mov a, #$ab
        mov y, #$02
        call init_test
        mov a, #$fe
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        cmp $02, #$01
        call save_results ; now a = result psw
        cmp a, #$a1
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$fe
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ac:
        mov a, #$ac
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$fe
        mov y, #$56
        pop psw
        cmp x, #$01
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$fe
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ad:
        mov a, #$ad
        mov y, #$02
        call init_test
        mov $01, #$01
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$fe
        mov y, #$56
        pop psw
        cmp x, $01
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$fe
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ae:
        mov a, #$ae
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$fe
        mov y, #$56
        pop psw
        cmp x, $02
        call save_results ; now a = result psw
        cmp a, #$a1
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$fe
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02af:
        mov a, #$af
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$fe
        mov y, #$56
        pop psw
        cmp x, $103
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$fe
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02b0:
        mov a, #$b0
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$fe
        pop psw
        cmp y, #$01
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$fe
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02b1:
        mov a, #$b1
        mov y, #$02
        call init_test
        mov $01, #$01
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$fe
        pop psw
        cmp y, $01
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$fe
        bne .to_fail
        cmp $01, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02b2:
        mov a, #$b2
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$fe
        pop psw
        cmp y, $02
        call save_results ; now a = result psw
        cmp a, #$a1
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$fe
        bne .to_fail
        mov a, $0102
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02b3:
        mov a, #$b3
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$fe
        pop psw
        cmp y, $103
        call save_results ; now a = result psw
        cmp a, #$81
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$fe
        bne .to_fail
        mov a, $0103
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02b4:
        mov a, #$b4
        mov y, #$02
        call init_test
        mov $01, #$45
        mov $02, #$12
        mov a, #$00
        push a
        mov a, #$45
        mov x, #$34
        mov y, #$23
        pop psw
        cmpw ya, $01
        call save_results ; now a = result psw
        cmp a, #$01
        bne .to_fail
        cmp result_a, #$45
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$23
        bne .to_fail
        cmp $01, #$45
        bne .to_fail
        cmp $02, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02b5:
        mov a, #$b5
        mov y, #$02
        call init_test
        mov a, #$45
        mov $01ff, a
        mov a, #$12
        mov $0100, a
        mov a, #$20
        push a
        mov a, #$45
        mov x, #$34
        mov y, #$23
        pop psw
        cmpw ya, $FF
        call save_results ; now a = result psw
        cmp a, #$21
        bne .to_fail
        cmp result_a, #$45
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$23
        bne .to_fail
        mov a, $01ff
        cmp a, #$45
        bne .to_fail
        mov a, $0100
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02b6:
        mov a, #$b6
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov $02, #$ff
        mov a, #$df
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$ff
        pop psw
        cmpw ya, $01
        call save_results ; now a = result psw
        cmp a, #$5f
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$ff
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02b7:
        mov a, #$b7
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $01ff, a
        mov a, #$ff
        mov $0100, a
        mov a, #$ff
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$ff
        pop psw
        cmpw ya, $FF
        call save_results ; now a = result psw
        cmp a, #$7f
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$ff
        bne .to_fail
        mov a, $01ff
        cmp a, #$ff
        bne .to_fail
        mov a, $0100
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02b8:
        mov a, #$b8
        mov y, #$02
        call init_test
        mov $01, #$01
        mov $02, #$00
        mov a, #$df
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$ff
        pop psw
        cmpw ya, $01
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$ff
        bne .to_fail
        cmp $01, #$01
        bne .to_fail
        cmp $02, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02b9:
        mov a, #$b9
        mov y, #$02
        call init_test
        mov a, #$01
        mov $01ff, a
        mov a, #$00
        mov $0100, a
        mov a, #$ff
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$ff
        pop psw
        cmpw ya, $FF
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$ff
        bne .to_fail
        mov a, $01ff
        cmp a, #$01
        bne .to_fail
        mov a, $0100
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ba:
        mov a, #$ba
        mov y, #$02
        call init_test
        mov $01, #$00
        mov $02, #$80
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$7f
        pop psw
        cmpw ya, $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$7f
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        cmp $02, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02bb:
        mov a, #$bb
        mov y, #$02
        call init_test
        mov a, #$00
        mov $01ff, a
        mov a, #$80
        mov $0100, a
        mov a, #$20
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$7f
        pop psw
        cmpw ya, $FF
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$7f
        bne .to_fail
        mov a, $01ff
        cmp a, #$00
        bne .to_fail
        mov a, $0100
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02bc:
        mov a, #$bc
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        bra .branch
    .ok:
        bra .ok2
    .branch:
        dbnz y, .ok
        call save_results
        bra .to_fail
    .ok2:
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02bd:
        mov a, #$bd
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        dbnz y, .ok
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02be:
        mov a, #$be
        mov y, #$02
        call init_test
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$81
        pop psw
        bra .branch
    .ok:
        bra .ok2
    .branch:
        dbnz y, .ok
        call save_results
        bra .to_fail
    .ok2:
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02bf:
        mov a, #$bf
        mov y, #$02
        call init_test
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$81
        pop psw
        dbnz y, .ok
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02c0:
        mov a, #$c0
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$01
        pop psw
        dbnz y, .not_ok
        bra .ok
    .not_ok:
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02c1:
        mov a, #$c1
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        bra .branch
    .ok:
        bra .ok2
    .branch:
        dbnz $01, .ok
        call save_results
        bra .to_fail
    .ok2:
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02c2:
        mov a, #$c2
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dbnz $01, .ok
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02c3:
        mov a, #$c3
        mov y, #$02
        call init_test
        mov $01, #$81
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        bra .branch
    .ok:
        bra .ok2
    .branch:
        dbnz $01, .ok
        call save_results
        bra .to_fail
    .ok2:
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02c4:
        mov a, #$c4
        mov y, #$02
        call init_test
        mov $01, #$81
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dbnz $01, .ok
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02c5:
        mov a, #$c5
        mov y, #$02
        call init_test
        mov $01, #$01
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dbnz $01, .not_ok
        bra .ok
    .not_ok:
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02c6:
        mov a, #$c6
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        bra .branch
    .ok:
        bra .ok2
    .branch:
        dbnz $02, .ok
        call save_results
        bra .to_fail
    .ok2:
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02c7:
        mov a, #$c7
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dbnz $02, .ok
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02c8:
        mov a, #$c8
        mov y, #$02
        call init_test
        mov a, #$81
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        bra .branch
    .ok:
        bra .ok2
    .branch:
        dbnz $02, .ok
        call save_results
        bra .to_fail
    .ok2:
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02c9:
        mov a, #$c9
        mov y, #$02
        call init_test
        mov a, #$81
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dbnz $02, .ok
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ca:
        mov a, #$ca
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dbnz $02, .not_ok
        bra .ok
    .not_ok:
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02cb:
        mov a, #$cb
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$56
        pop psw
        dec a
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02cc:
        mov a, #$cc
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dec $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02cd:
        mov a, #$cd
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dec $02
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ce:
        mov a, #$ce
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        dec $ff+x
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02cf:
        mov a, #$cf
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        dec $ff+x
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02d0:
        mov a, #$d0
        mov y, #$02
        call init_test
        mov a, #$00
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dec $103
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02d1:
        mov a, #$d1
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        dec x
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$ff
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02d2:
        mov a, #$d2
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        dec y
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02d3:
        mov a, #$d3
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$01
        mov x, #$34
        mov y, #$56
        pop psw
        dec a
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02d4:
        mov a, #$d4
        mov y, #$02
        call init_test
        mov $01, #$01
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dec $01
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02d5:
        mov a, #$d5
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dec $02
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02d6:
        mov a, #$d6
        mov y, #$02
        call init_test
        mov $01, #$01
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        dec $ff+x
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02d7:
        mov a, #$d7
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        dec $ff+x
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02d8:
        mov a, #$d8
        mov y, #$02
        call init_test
        mov a, #$01
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dec $103
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02d9:
        mov a, #$d9
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$56
        pop psw
        dec x
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02da:
        mov a, #$da
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$01
        pop psw
        dec y
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02db:
        mov a, #$db
        mov y, #$02
        call init_test
        mov a, #$df
        push a
        mov a, #$80
        mov x, #$34
        mov y, #$56
        pop psw
        dec a
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$7f
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02dc:
        mov a, #$dc
        mov y, #$02
        call init_test
        mov $01, #$80
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dec $01
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02dd:
        mov a, #$dd
        mov y, #$02
        call init_test
        mov a, #$80
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dec $02
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02de:
        mov a, #$de
        mov y, #$02
        call init_test
        mov $01, #$80
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        dec $ff+x
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02df:
        mov a, #$df
        mov y, #$02
        call init_test
        mov a, #$80
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        dec $ff+x
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02e0:
        mov a, #$e0
        mov y, #$02
        call init_test
        mov a, #$80
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        dec $103
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02e1:
        mov a, #$e1
        mov y, #$02
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$80
        mov y, #$56
        pop psw
        dec x
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$7f
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02e2:
        mov a, #$e2
        mov y, #$02
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$80
        pop psw
        dec y
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02e3:
        mov a, #$e3
        mov y, #$02
        call init_test
        mov $01, #$01
        mov $02, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        decw $01
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        cmp $02, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02e4:
        mov a, #$e4
        mov y, #$02
        call init_test
        mov a, #$01
        mov $01ff, a
        mov a, #$00
        mov $0100, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        decw $FF
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $01ff
        cmp a, #$00
        bne .to_fail
        mov a, $0100
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02e5:
        mov a, #$e5
        mov y, #$02
        call init_test
        mov $01, #$00
        mov $02, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        decw $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02e6:
        mov a, #$e6
        mov y, #$02
        call init_test
        mov a, #$00
        mov $01ff, a
        mov a, #$00
        mov $0100, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        decw $FF
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $01ff
        cmp a, #$ff
        bne .to_fail
        mov a, $0100
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02e7:
        mov a, #$e7
        mov y, #$02
        call init_test
        mov $01, #$01
        mov $02, #$01
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        decw $01
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02e8:
        mov a, #$e8
        mov y, #$02
        call init_test
        mov a, #$01
        mov $01ff, a
        mov a, #$01
        mov $0100, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        decw $FF
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $01ff
        cmp a, #$00
        bne .to_fail
        mov a, $0100
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02e9:
        mov a, #$e9
        mov y, #$02
        call init_test
        mov $01, #$80
        mov $02, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        decw $01
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$7f
        bne .to_fail
        cmp $02, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ea:
        mov a, #$ea
        mov y, #$02
        call init_test
        mov a, #$80
        mov $01ff, a
        mov a, #$00
        mov $0100, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        decw $FF
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $01ff
        cmp a, #$7f
        bne .to_fail
        mov a, $0100
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02eb:
        mov a, #$eb
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, #$ff
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ec:
        mov a, #$ec
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov $02, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$02
        pop psw
        eor (x), (y)
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ed:
        mov a, #$ed
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$01
        mov y, #$56
        pop psw
        eor a, (x)
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ee:
        mov a, #$ee
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$ff
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$05
        pop psw
        eor a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$ff
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ef:
        mov a, #$ef
        mov y, #$02
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$ff
        mov $0102, a
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$02
        mov y, #$56
        pop psw
        eor a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02f0:
        mov a, #$f0
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $01
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02f1:
        mov a, #$f1
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $02
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02f2:
        mov a, #$f2
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$02
        mov y, #$56
        pop psw
        eor a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02f3:
        mov a, #$f3
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$ff
        mov x, #$03
        mov y, #$56
        pop psw
        eor a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02f4:
        mov a, #$f4
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $103
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02f5:
        mov a, #$f5
        mov y, #$02
        call init_test
        mov $04, #$ff
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$05
        mov y, #$56
        pop psw
        eor a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02f6:
        mov a, #$f6
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0104, a
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$01
        pop psw
        eor a, $103+y
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02f7:
        mov a, #$f7
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov $02, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $01, $02
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02f8:
        mov a, #$f8
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$ff
        mov $0103, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $02, $03
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        mov a, $0103
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02f9:
        mov a, #$f9
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $01, #$ff
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02fa:
        mov a, #$fa
        mov y, #$02
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $02, #$ff
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02fb:
        mov a, #$fb
        mov y, #$02
        call init_test
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, #$00
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02fc:
        mov a, #$fc
        mov y, #$02
        call init_test
        mov $01, #$ff
        mov $02, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$02
        pop psw
        eor (x), (y)
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        cmp $02, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02fd:
        mov a, #$fd
        mov y, #$02
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$01
        mov y, #$56
        pop psw
        eor a, (x)
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02fe:
        mov a, #$fe
        mov y, #$02
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$00
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$05
        pop psw
        eor a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$00
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test02ff:
        mov a, #$ff
        mov y, #$02
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$00
        mov $0102, a
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$02
        mov y, #$56
        pop psw
        eor a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0300:
        mov a, #$00
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0301:
        mov a, #$01
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $02
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0302:
        mov a, #$02
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$02
        mov y, #$56
        pop psw
        eor a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0303:
        mov a, #$03
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$ff
        mov x, #$03
        mov y, #$56
        pop psw
        eor a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0304:
        mov a, #$04
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $103
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0305:
        mov a, #$05
        mov y, #$03
        call init_test
        mov $04, #$00
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$05
        mov y, #$56
        pop psw
        eor a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0306:
        mov a, #$06
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0104, a
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$01
        pop psw
        eor a, $103+y
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$ff
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0307:
        mov a, #$07
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov $02, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $01, $02
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        cmp $02, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0308:
        mov a, #$08
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$00
        mov $0103, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $02, $03
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0309:
        mov a, #$09
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $01, #$00
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test030a:
        mov a, #$0a
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $02, #$00
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test030b:
        mov a, #$0b
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$55
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, #$ff
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test030c:
        mov a, #$0c
        mov y, #$03
        call init_test
        mov $01, #$55
        mov $02, #$ff
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$02
        pop psw
        eor (x), (y)
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$aa
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test030d:
        mov a, #$0d
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$df
        push a
        mov a, #$55
        mov x, #$01
        mov y, #$56
        pop psw
        eor a, (x)
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test030e:
        mov a, #$0e
        mov y, #$03
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$ff
        mov a, #$df
        push a
        mov a, #$55
        mov x, #$34
        mov y, #$05
        pop psw
        eor a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$ff
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test030f:
        mov a, #$0f
        mov y, #$03
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$ff
        mov $0102, a
        mov a, #$df
        push a
        mov a, #$55
        mov x, #$02
        mov y, #$56
        pop psw
        eor a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0310:
        mov a, #$10
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$df
        push a
        mov a, #$55
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $01
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0311:
        mov a, #$11
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$55
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $02
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0312:
        mov a, #$12
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$df
        push a
        mov a, #$55
        mov x, #$02
        mov y, #$56
        pop psw
        eor a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0313:
        mov a, #$13
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$55
        mov x, #$03
        mov y, #$56
        pop psw
        eor a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0314:
        mov a, #$14
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$55
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $103
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0315:
        mov a, #$15
        mov y, #$03
        call init_test
        mov $04, #$ff
        mov a, #$df
        push a
        mov a, #$55
        mov x, #$05
        mov y, #$56
        pop psw
        eor a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0316:
        mov a, #$16
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0104, a
        mov a, #$df
        push a
        mov a, #$55
        mov x, #$34
        mov y, #$01
        pop psw
        eor a, $103+y
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$aa
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0317:
        mov a, #$17
        mov y, #$03
        call init_test
        mov $01, #$55
        mov $02, #$ff
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $01, $02
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$aa
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0318:
        mov a, #$18
        mov y, #$03
        call init_test
        mov a, #$55
        mov $0102, a
        mov a, #$ff
        mov $0103, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $02, $03
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$aa
        bne .to_fail
        mov a, $0103
        cmp a, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0319:
        mov a, #$19
        mov y, #$03
        call init_test
        mov $01, #$55
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $01, #$ff
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$aa
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test031a:
        mov a, #$1a
        mov y, #$03
        call init_test
        mov a, #$55
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $02, #$ff
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$aa
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test031b:
        mov a, #$1b
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$35
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, #$89
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test031c:
        mov a, #$1c
        mov y, #$03
        call init_test
        mov $01, #$35
        mov $02, #$89
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$02
        pop psw
        eor (x), (y)
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$bc
        bne .to_fail
        cmp $02, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test031d:
        mov a, #$1d
        mov y, #$03
        call init_test
        mov $01, #$89
        mov a, #$df
        push a
        mov a, #$35
        mov x, #$01
        mov y, #$56
        pop psw
        eor a, (x)
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test031e:
        mov a, #$1e
        mov y, #$03
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$89
        mov a, #$df
        push a
        mov a, #$35
        mov x, #$34
        mov y, #$05
        pop psw
        eor a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$89
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test031f:
        mov a, #$1f
        mov y, #$03
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$89
        mov $0102, a
        mov a, #$df
        push a
        mov a, #$35
        mov x, #$02
        mov y, #$56
        pop psw
        eor a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$89
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0320:
        mov a, #$20
        mov y, #$03
        call init_test
        mov $01, #$89
        mov a, #$df
        push a
        mov a, #$35
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $01
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0321:
        mov a, #$21
        mov y, #$03
        call init_test
        mov a, #$89
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$35
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $02
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0322:
        mov a, #$22
        mov y, #$03
        call init_test
        mov $01, #$89
        mov a, #$df
        push a
        mov a, #$35
        mov x, #$02
        mov y, #$56
        pop psw
        eor a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0323:
        mov a, #$23
        mov y, #$03
        call init_test
        mov a, #$89
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$35
        mov x, #$03
        mov y, #$56
        pop psw
        eor a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0324:
        mov a, #$24
        mov y, #$03
        call init_test
        mov a, #$89
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$35
        mov x, #$34
        mov y, #$56
        pop psw
        eor a, $103
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0325:
        mov a, #$25
        mov y, #$03
        call init_test
        mov $04, #$89
        mov a, #$df
        push a
        mov a, #$35
        mov x, #$05
        mov y, #$56
        pop psw
        eor a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0326:
        mov a, #$26
        mov y, #$03
        call init_test
        mov a, #$89
        mov $0104, a
        mov a, #$df
        push a
        mov a, #$35
        mov x, #$34
        mov y, #$01
        pop psw
        eor a, $103+y
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$bc
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0327:
        mov a, #$27
        mov y, #$03
        call init_test
        mov $01, #$35
        mov $02, #$89
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $01, $02
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$bc
        bne .to_fail
        cmp $02, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0328:
        mov a, #$28
        mov y, #$03
        call init_test
        mov a, #$35
        mov $0102, a
        mov a, #$89
        mov $0103, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $02, $03
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$bc
        bne .to_fail
        mov a, $0103
        cmp a, #$89
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0329:
        mov a, #$29
        mov y, #$03
        call init_test
        mov $01, #$35
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $01, #$89
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$bc
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test032a:
        mov a, #$2a
        mov y, #$03
        call init_test
        mov a, #$35
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor $02, #$89
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$bc
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test032b:
        mov a, #$2b
        mov y, #$03
        call init_test
        mov a, #$04
        mov $0102, a
        mov a, #$01
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor1 C, $102.2
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$04
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test032c:
        mov a, #$2c
        mov y, #$03
        call init_test
        mov a, #$04
        mov $0102, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor1 C, $102.2
        call save_results ; now a = result psw
        cmp a, #$01
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$04
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test032d:
        mov a, #$2d
        mov y, #$03
        call init_test
        mov a, #$fb
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor1 C, $102.2
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$fb
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test032e:
        mov a, #$2e
        mov y, #$03
        call init_test
        mov a, #$fb
        mov $0102, a
        mov a, #$fe
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        eor1 C, $102.2
        call save_results ; now a = result psw
        cmp a, #$fe
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$fb
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test032f:
        mov a, #$2f
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$56
        pop psw
        inc a
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$01
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0330:
        mov a, #$30
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        inc $01
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0331:
        mov a, #$31
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        inc $02
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0332:
        mov a, #$32
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        inc $ff+x
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0333:
        mov a, #$33
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        inc $ff+x
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0334:
        mov a, #$34
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        inc $103
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0335:
        mov a, #$35
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        inc x
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0336:
        mov a, #$36
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        inc y
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0337:
        mov a, #$37
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$56
        pop psw
        inc a
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0338:
        mov a, #$38
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        inc $01
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0339:
        mov a, #$39
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        inc $02
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test033a:
        mov a, #$3a
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        inc $ff+x
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test033b:
        mov a, #$3b
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        inc $ff+x
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test033c:
        mov a, #$3c
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        inc $103
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test033d:
        mov a, #$3d
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$ff
        mov y, #$56
        pop psw
        inc x
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test033e:
        mov a, #$3e
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$ff
        pop psw
        inc y
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test033f:
        mov a, #$3f
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$80
        mov x, #$34
        mov y, #$56
        pop psw
        inc a
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$81
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0340:
        mov a, #$40
        mov y, #$03
        call init_test
        mov $01, #$80
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        inc $01
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$81
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0341:
        mov a, #$41
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        inc $02
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$81
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0342:
        mov a, #$42
        mov y, #$03
        call init_test
        mov $01, #$80
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        inc $ff+x
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$81
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0343:
        mov a, #$43
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        inc $ff+x
        call save_results ; now a = result psw
        cmp a, #$fd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$81
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0344:
        mov a, #$44
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        inc $103
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$81
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0345:
        mov a, #$45
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$80
        mov y, #$56
        pop psw
        inc x
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$81
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0346:
        mov a, #$46
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$80
        pop psw
        inc y
        call save_results ; now a = result psw
        cmp a, #$dd
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$81
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0347:
        mov a, #$47
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov $02, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        incw $01
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        cmp $02, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0348:
        mov a, #$48
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $01ff, a
        mov a, #$ff
        mov $0100, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        incw $FF
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $01ff
        cmp a, #$00
        bne .to_fail
        mov a, $0100
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0349:
        mov a, #$49
        mov y, #$03
        call init_test
        mov $01, #$00
        mov $02, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        incw $01
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$01
        bne .to_fail
        cmp $02, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test034a:
        mov a, #$4a
        mov y, #$03
        call init_test
        mov a, #$00
        mov $01ff, a
        mov a, #$00
        mov $0100, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        incw $FF
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $01ff
        cmp a, #$01
        bne .to_fail
        mov a, $0100
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test034b:
        mov a, #$4b
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov $02, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        incw $01
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test034c:
        mov a, #$4c
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $01ff, a
        mov a, #$00
        mov $0100, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        incw $FF
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $01ff
        cmp a, #$00
        bne .to_fail
        mov a, $0100
        cmp a, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test034d:
        mov a, #$4d
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov $02, #$7f
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        incw $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        cmp $02, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test034e:
        mov a, #$4e
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $01ff, a
        mov a, #$7f
        mov $0100, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        incw $FF
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $01ff
        cmp a, #$00
        bne .to_fail
        mov a, $0100
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test034f:
        mov a, #$4f
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        jmp .ok
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0350:
        mov a, #$50
        mov y, #$03
        call init_test
        mov a, #(.ok & $FF)
        mov $FFFF, a
        mov $00, #(.ok >> 8)
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$ff
        mov y, #$56
        pop psw
        jmp [$FF00+x]
        call save_results
        bra .to_fail
    .ok:
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$ff
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0351:
        mov a, #$51
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$ff
        mov x, #$34
        mov y, #$56
        pop psw
        lsr a
        call save_results ; now a = result psw
        cmp a, #$01
        bne .to_fail
        cmp result_a, #$7f
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0352:
        mov a, #$52
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        lsr $01
        call save_results ; now a = result psw
        cmp a, #$01
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0353:
        mov a, #$53
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        lsr $02
        call save_results ; now a = result psw
        cmp a, #$21
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0354:
        mov a, #$54
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        lsr $ff+x
        call save_results ; now a = result psw
        cmp a, #$01
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0355:
        mov a, #$55
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        lsr $ff+x
        call save_results ; now a = result psw
        cmp a, #$21
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0356:
        mov a, #$56
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        lsr $103
        call save_results ; now a = result psw
        cmp a, #$01
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$7f
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0357:
        mov a, #$57
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$01
        mov x, #$34
        mov y, #$56
        pop psw
        lsr a
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0358:
        mov a, #$58
        mov y, #$03
        call init_test
        mov $01, #$01
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        lsr $01
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0359:
        mov a, #$59
        mov y, #$03
        call init_test
        mov a, #$01
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        lsr $02
        call save_results ; now a = result psw
        cmp a, #$23
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test035a:
        mov a, #$5a
        mov y, #$03
        call init_test
        mov $01, #$01
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        lsr $ff+x
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test035b:
        mov a, #$5b
        mov y, #$03
        call init_test
        mov a, #$01
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        lsr $ff+x
        call save_results ; now a = result psw
        cmp a, #$23
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test035c:
        mov a, #$5c
        mov y, #$03
        call init_test
        mov a, #$01
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        lsr $103
        call save_results ; now a = result psw
        cmp a, #$03
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test035d:
        mov a, #$5d
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$06
        mov x, #$34
        mov y, #$56
        pop psw
        lsr a
        call save_results ; now a = result psw
        cmp a, #$5c
        bne .to_fail
        cmp result_a, #$03
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test035e:
        mov a, #$5e
        mov y, #$03
        call init_test
        mov $01, #$06
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        lsr $01
        call save_results ; now a = result psw
        cmp a, #$5c
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$03
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test035f:
        mov a, #$5f
        mov y, #$03
        call init_test
        mov a, #$06
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        lsr $02
        call save_results ; now a = result psw
        cmp a, #$7c
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$03
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0360:
        mov a, #$60
        mov y, #$03
        call init_test
        mov $01, #$06
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        lsr $ff+x
        call save_results ; now a = result psw
        cmp a, #$5c
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$03
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0361:
        mov a, #$61
        mov y, #$03
        call init_test
        mov a, #$06
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        lsr $ff+x
        call save_results ; now a = result psw
        cmp a, #$7c
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$03
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0362:
        mov a, #$62
        mov y, #$03
        call init_test
        mov a, #$06
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        lsr $103
        call save_results ; now a = result psw
        cmp a, #$5c
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$03
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0363:
        mov a, #$63
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, #$12
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0364:
        mov a, #$64
        mov y, #$03
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$01
        mov y, #$56
        pop psw
        mov a, (x)
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0365:
        mov a, #$65
        mov y, #$03
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$12
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$05
        pop psw
        mov a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$12
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0366:
        mov a, #$66
        mov y, #$03
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$12
        mov $0102, a
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$02
        mov y, #$56
        pop psw
        mov a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0367:
        mov a, #$67
        mov y, #$03
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, $01
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0368:
        mov a, #$68
        mov y, #$03
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, $02
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0369:
        mov a, #$69
        mov y, #$03
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$02
        mov y, #$56
        pop psw
        mov a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test036a:
        mov a, #$6a
        mov y, #$03
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$00
        mov x, #$03
        mov y, #$56
        pop psw
        mov a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test036b:
        mov a, #$6b
        mov y, #$03
        call init_test
        mov a, #$12
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, $103
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test036c:
        mov a, #$6c
        mov y, #$03
        call init_test
        mov $04, #$12
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$05
        mov y, #$56
        pop psw
        mov a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test036d:
        mov a, #$6d
        mov y, #$03
        call init_test
        mov a, #$12
        mov $0104, a
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$01
        pop psw
        mov a, $103+y
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test036e:
        mov a, #$6e
        mov y, #$03
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$01
        mov y, #$56
        pop psw
        mov a, (x+)
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test036f:
        mov a, #$6f
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, #$80
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0370:
        mov a, #$70
        mov y, #$03
        call init_test
        mov $01, #$80
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$56
        pop psw
        mov a, (x)
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0371:
        mov a, #$71
        mov y, #$03
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$80
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$05
        pop psw
        mov a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$80
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0372:
        mov a, #$72
        mov y, #$03
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$80
        mov $0102, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        mov a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$80
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0373:
        mov a, #$73
        mov y, #$03
        call init_test
        mov $01, #$80
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0374:
        mov a, #$74
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, $02
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0375:
        mov a, #$75
        mov y, #$03
        call init_test
        mov $01, #$80
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        mov a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0376:
        mov a, #$76
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        mov a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0377:
        mov a, #$77
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, $103
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0378:
        mov a, #$78
        mov y, #$03
        call init_test
        mov $04, #$80
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$05
        mov y, #$56
        pop psw
        mov a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0379:
        mov a, #$79
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0104, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$01
        pop psw
        mov a, $103+y
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test037a:
        mov a, #$7a
        mov y, #$03
        call init_test
        mov $01, #$80
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$56
        pop psw
        mov a, (x+)
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test037b:
        mov a, #$7b
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, #$00
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test037c:
        mov a, #$7c
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$56
        pop psw
        mov a, (x)
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test037d:
        mov a, #$7d
        mov y, #$03
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$05
        pop psw
        mov a, [$01]+y
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$00
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test037e:
        mov a, #$7e
        mov y, #$03
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$00
        mov $0102, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        mov a, [$ff+x]
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test037f:
        mov a, #$7f
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, $01
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0380:
        mov a, #$80
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, $02
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0381:
        mov a, #$81
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        mov a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0382:
        mov a, #$82
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        mov a, $ff+x
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0383:
        mov a, #$83
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov a, $103
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0384:
        mov a, #$84
        mov y, #$03
        call init_test
        mov $04, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$05
        mov y, #$56
        pop psw
        mov a, $ffff+x
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0385:
        mov a, #$85
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0104, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$01
        pop psw
        mov a, $103+y
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0386:
        mov a, #$86
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$56
        pop psw
        mov a, (x+)
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0387:
        mov a, #$87
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$56
        pop psw
        mov (x), a
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0388:
        mov a, #$88
        mov y, #$03
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$05
        pop psw
        mov [$01]+y, a
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$12
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0389:
        mov a, #$89
        mov y, #$03
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$00
        mov $0102, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        mov [$ff+x], a
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test038a:
        mov a, #$8a
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $01, a
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test038b:
        mov a, #$8b
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $02, a
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test038c:
        mov a, #$8c
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        mov $ff+x, a
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test038d:
        mov a, #$8d
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        mov $ff+x, a
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test038e:
        mov a, #$8e
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $103, a
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test038f:
        mov a, #$8f
        mov y, #$03
        call init_test
        mov $04, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$05
        mov y, #$56
        pop psw
        mov $ffff+x, a
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0390:
        mov a, #$90
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0104, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$01
        pop psw
        mov $103+y, a
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0391:
        mov a, #$91
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$01
        mov y, #$56
        pop psw
        mov (x+), a
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0392:
        mov a, #$92
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$01
        mov y, #$56
        pop psw
        mov (x), a
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$01
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0393:
        mov a, #$93
        mov y, #$03
        call init_test
        mov $01, #$fe
        mov $02, #$ff
        mov $03, #$ff
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$05
        pop psw
        mov [$01]+y, a
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$05
        bne .to_fail
        cmp $03, #$00
        bne .to_fail
        cmp $01, #$fe
        bne .to_fail
        cmp $02, #$ff
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0394:
        mov a, #$94
        mov y, #$03
        call init_test
        mov $01, #$02
        mov $02, #$01
        mov a, #$ff
        mov $0102, a
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$02
        mov y, #$56
        pop psw
        mov [$ff+x], a
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        cmp $01, #$02
        bne .to_fail
        cmp $02, #$01
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0395:
        mov a, #$95
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$56
        pop psw
        mov $01, a
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0396:
        mov a, #$96
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$56
        pop psw
        mov $02, a
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0397:
        mov a, #$97
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$02
        mov y, #$56
        pop psw
        mov $ff+x, a
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0398:
        mov a, #$98
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$00
        mov x, #$03
        mov y, #$56
        pop psw
        mov $ff+x, a
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test0399:
        mov a, #$99
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$56
        pop psw
        mov $103, a
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test039a:
        mov a, #$9a
        mov y, #$03
        call init_test
        mov $04, #$ff
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$05
        mov y, #$56
        pop psw
        mov $ffff+x, a
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$05
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $04, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test039b:
        mov a, #$9b
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0104, a
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$01
        pop psw
        mov $103+y, a
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$01
        bne .to_fail
        mov a, $0104
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test039c:
        mov a, #$9c
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$01
        mov y, #$56
        pop psw
        mov (x+), a
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test039d:
        mov a, #$9d
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        mov x, #$12
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test039e:
        mov a, #$9e
        mov y, #$03
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        mov x, $01
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test039f:
        mov a, #$9f
        mov y, #$03
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        mov x, $02
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03a0:
        mov a, #$a0
        mov y, #$03
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$02
        pop psw
        mov x, $ff+y
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03a1:
        mov a, #$a1
        mov y, #$03
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$03
        pop psw
        mov x, $ff+y
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$03
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03a2:
        mov a, #$a2
        mov y, #$03
        call init_test
        mov a, #$12
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        mov x, $103
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03a3:
        mov a, #$a3
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov x, #$80
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$80
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03a4:
        mov a, #$a4
        mov y, #$03
        call init_test
        mov $01, #$80
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov x, $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$80
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03a5:
        mov a, #$a5
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov x, $02
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$80
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03a6:
        mov a, #$a6
        mov y, #$03
        call init_test
        mov $01, #$80
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$02
        pop psw
        mov x, $ff+y
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$80
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03a7:
        mov a, #$a7
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$03
        pop psw
        mov x, $ff+y
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$80
        bne .to_fail
        cmp y, #$03
        bne .to_fail
        mov a, $0102
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03a8:
        mov a, #$a8
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov x, $103
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$80
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03a9:
        mov a, #$a9
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov x, #$00
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03aa:
        mov a, #$aa
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov x, $01
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03ab:
        mov a, #$ab
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov x, $02
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03ac:
        mov a, #$ac
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$02
        pop psw
        mov x, $ff+y
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03ad:
        mov a, #$ad
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$03
        pop psw
        mov x, $ff+y
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$03
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03ae:
        mov a, #$ae
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov x, $103
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03af:
        mov a, #$af
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        mov y, #$12
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03b0:
        mov a, #$b0
        mov y, #$03
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        mov y, $01
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03b1:
        mov a, #$b1
        mov y, #$03
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        mov y, $02
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03b2:
        mov a, #$b2
        mov y, #$03
        call init_test
        mov $01, #$12
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$00
        pop psw
        mov y, $ff+x
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03b3:
        mov a, #$b3
        mov y, #$03
        call init_test
        mov a, #$12
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$00
        pop psw
        mov y, $ff+x
        call save_results ; now a = result psw
        cmp a, #$7d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03b4:
        mov a, #$b4
        mov y, #$03
        call init_test
        mov a, #$12
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        mov y, $103
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03b5:
        mov a, #$b5
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov y, #$80
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03b6:
        mov a, #$b6
        mov y, #$03
        call init_test
        mov $01, #$80
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov y, $01
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$80
        bne .to_fail
        cmp $01, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03b7:
        mov a, #$b7
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov y, $02
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$80
        bne .to_fail
        mov a, $0102
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03b8:
        mov a, #$b8
        mov y, #$03
        call init_test
        mov $01, #$80
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        mov y, $ff+x
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$80
        bne .to_fail
        cmp $01, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03b9:
        mov a, #$b9
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        mov y, $ff+x
        call save_results ; now a = result psw
        cmp a, #$a0
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$80
        bne .to_fail
        mov a, $0102
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03ba:
        mov a, #$ba
        mov y, #$03
        call init_test
        mov a, #$80
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov y, $103
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$80
        bne .to_fail
        mov a, $0103
        cmp a, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03bb:
        mov a, #$bb
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov y, #$00
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03bc:
        mov a, #$bc
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov y, $01
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03bd:
        mov a, #$bd
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov y, $02
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03be:
        mov a, #$be
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$56
        pop psw
        mov y, $ff+x
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03bf:
        mov a, #$bf
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$56
        pop psw
        mov y, $ff+x
        call save_results ; now a = result psw
        cmp a, #$22
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03c0:
        mov a, #$c0
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov y, $103
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03c1:
        mov a, #$c1
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        mov $01, x
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03c2:
        mov a, #$c2
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        mov $02, x
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03c3:
        mov a, #$c3
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$02
        pop psw
        mov $ff+y, x
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03c4:
        mov a, #$c4
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$03
        pop psw
        mov $ff+y, x
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$03
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03c5:
        mov a, #$c5
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$12
        mov y, #$56
        pop psw
        mov $103, x
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03c6:
        mov a, #$c6
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        mov $01, x
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03c7:
        mov a, #$c7
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        mov $02, x
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03c8:
        mov a, #$c8
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$02
        pop psw
        mov $ff+y, x
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$02
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03c9:
        mov a, #$c9
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$03
        pop psw
        mov $ff+y, x
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$03
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03ca:
        mov a, #$ca
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        mov $103, x
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03cb:
        mov a, #$cb
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        mov $01, y
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03cc:
        mov a, #$cc
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        mov $02, y
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03cd:
        mov a, #$cd
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$12
        pop psw
        mov $ff+x, y
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03ce:
        mov a, #$ce
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$12
        pop psw
        mov $ff+x, y
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03cf:
        mov a, #$cf
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0103, a
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$12
        pop psw
        mov $103, y
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03d0:
        mov a, #$d0
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        mov $01, y
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03d1:
        mov a, #$d1
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        mov $02, y
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03d2:
        mov a, #$d2
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$02
        mov y, #$00
        pop psw
        mov $ff+x, y
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$02
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03d3:
        mov a, #$d3
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$03
        mov y, #$00
        pop psw
        mov $ff+x, y
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$03
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03d4:
        mov a, #$d4
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0103, a
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        mov $103, y
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03d5:
        mov a, #$d5
        mov y, #$03
        call init_test
        mov $01, #$00
        mov $02, #$12
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $01, $02
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        cmp $02, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03d6:
        mov a, #$d6
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$12
        mov $0103, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $02, $03
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        mov a, $0103
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03d7:
        mov a, #$d7
        mov y, #$03
        call init_test
        mov $01, #$00
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $01, #$12
        call save_results ; now a = result psw
        cmp a, #$df
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03d8:
        mov a, #$d8
        mov y, #$03
        call init_test
        mov a, #$00
        mov $0102, a
        mov a, #$ff
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $02, #$12
        call save_results ; now a = result psw
        cmp a, #$ff
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03d9:
        mov a, #$d9
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov $02, #$00
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $01, $02
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        cmp $02, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03da:
        mov a, #$da
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$00
        mov $0103, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $02, $03
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        mov a, $0103
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03db:
        mov a, #$db
        mov y, #$03
        call init_test
        mov $01, #$ff
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $01, #$00
        call save_results ; now a = result psw
        cmp a, #$00
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        cmp $01, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03dc:
        mov a, #$dc
        mov y, #$03
        call init_test
        mov a, #$ff
        mov $0102, a
        mov a, #$20
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$56
        pop psw
        mov $02, #$00
        call save_results ; now a = result psw
        cmp a, #$20
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        mov a, $0102
        cmp a, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03dd:
        mov a, #$dd
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$12
        mov y, #$56
        pop psw
        mov a, x
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03de:
        mov a, #$de
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        mov a, x
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03df:
        mov a, #$df
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$80
        mov y, #$56
        pop psw
        mov a, x
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$80
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03e0:
        mov a, #$e0
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$12
        pop psw
        mov a, y
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03e1:
        mov a, #$e1
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        mov a, y
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03e2:
        mov a, #$e2
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$80
        pop psw
        mov a, y
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$80
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03e3:
        mov a, #$e3
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$00
        mov y, #$56
        pop psw
        mov x, a
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$12
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03e4:
        mov a, #$e4
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$12
        mov y, #$56
        pop psw
        mov x, a
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$00
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03e5:
        mov a, #$e5
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$80
        mov x, #$12
        mov y, #$56
        pop psw
        mov x, a
        call save_results ; now a = result psw
        cmp a, #$80
        bne .to_fail
        cmp result_a, #$80
        bne .to_fail
        cmp x, #$80
        bne .to_fail
        cmp y, #$56
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03e6:
        mov a, #$e6
        mov y, #$03
        call init_test
        mov a, #$df
        push a
        mov a, #$12
        mov x, #$34
        mov y, #$00
        pop psw
        mov y, a
        call save_results ; now a = result psw
        cmp a, #$5d
        bne .to_fail
        cmp result_a, #$12
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$12
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
test03e7:
        mov a, #$e7
        mov y, #$03
        call init_test
        mov a, #$00
        push a
        mov a, #$00
        mov x, #$34
        mov y, #$12
        pop psw
        mov y, a
        call save_results ; now a = result psw
        cmp a, #$02
        bne .to_fail
        cmp result_a, #$00
        bne .to_fail
        cmp x, #$34
        bne .to_fail
        cmp y, #$00
        bne .to_fail
        bra .next_test
    .to_fail:
        jmp fail
    .next_test:
    
        jmp success
